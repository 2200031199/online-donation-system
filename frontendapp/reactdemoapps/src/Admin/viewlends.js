import React from 'react';
import config from '../config';

const Viewlends = () => {
    return (
        <div>
            view lends
            
        </div>
    );
}

export default Viewlends;
